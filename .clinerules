# Cline Rules - Interactive Treasure Hunt Game

## Memory Bank 優先原則

### 對話恢復策略
- 當對話重啟或清除後，優先讀取 `.claude/memory/` 資料夾中的內容來恢復記憶
- 除非明確要求，否則不要主動執行全局掃描或大規模讀取程式碼，以節省 Token

### 記憶文件讀取順序
1. **project_overview.md** - 快速了解專案基本資訊
2. **todos.md** - 查看待辦事項和優先級
3. **architecture.md** - 需要時才讀取架構細節
4. **features.md** - 需要時才讀取功能清單
5. **decisions.md** - 需要時才讀取決策記錄

### Token 節省策略
- ✅ 優先使用 Memory Bank 文件
- ✅ 只在需要時讀取具體代碼文件
- ✅ 避免不必要的全局掃描
- ✅ 使用 search_files 而非 read_file 進行探索
- ✅ 當用戶使用 `/memory` 命令時立即讀取記憶文件

### 工作流程
1. 對話開始 → 讀取 project_overview.md 和 todos.md
2. 用戶詢問問題 → 根據問題讀取相關記憶文件
3. 實現功能 → 讀取 architecture.md 和相關代碼
4. 完成工作 → 更新相應的記憶文件

## 專案特定規則

### 代碼修改
- 修改代碼前先查閱 decisions.md 了解設計原因
- 實現新功能後更新 features.md
- 重要決策記錄到 decisions.md

### 文件維護
- 保持 Memory Bank 文件更新
- 完成任務後更新 todos.md
- 記錄「為什麼」而不只是「是什麼」

### 技術棧約束
- React 18.3.1 + TypeScript
- Vite 6.3.5 構建工具
- Framer Motion 動畫
- Tailwind CSS 樣式
- 不引入新的重量級依賴除非必要

## 命令處理

### /memory 命令
立即讀取並總結：
- project_overview.md
- todos.md（高優先級任務）
- 最新的專案狀態

### /status 命令
報告：
- Memory Bank 狀態
- Git 狀態
- 待辦任務概覽
- 不需要讀取所有代碼

## 注意事項

- 本專案使用 Memory Bank 系統維護專案知識
- 所有重要資訊都記錄在 `.claude/memory/` 中
- 對話清除不會導致專案知識丟失
- 優先信任 Memory Bank 文件而非推測
